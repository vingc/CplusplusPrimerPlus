#include <iostream>
#include <string>
#include <vector>

using namespace std;

//int judge( int (*pf)( const char * ) );
int main() {
    //auto p1 = f1;
    // void (*p1)( applicant * a );
    // typedef const char * (*p2)( const applicant * a1, const applicant * a2 );
    // void (*ap[3])( applicant * a );
    // p2[10];

    
}